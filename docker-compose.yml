services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: crm_vet
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - crm_vet_postgres:/var/lib/postgresql/data

volumes:
  crm_vet_postgres:

